services:
  proxy:
    image: acrdiaanlnzndev.azurecr.io/http-proxy:${app_version}
    command: nginx -g "daemon off;"
    container_name: proxy
    hostname: proxy
    restart: always
    ports:
      - "8081:443"
